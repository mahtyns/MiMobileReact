import React from 'react'
import './styles/textStyling.css'

const SectionTitle = ({sectionTitle}) => {
  return (
    <div className='sectionTitleStyles'>{sectionTitle}</div>
  )
}

export default SectionTitle